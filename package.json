{
  "name": "hello-world-javascript-action",
  "version": "1.1.1",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --minify",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@actions/core": "^2.0.2",
    "@actions/github": "^6.0.1",
    "@vercel/ncc": "^0.38.4"
  },
  "private": true,
  "license": "MIT"
}
